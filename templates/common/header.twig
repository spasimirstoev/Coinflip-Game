<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

	<title>Spacy's Coinflip Game</title>
	<link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/custom.css">
</head>
<body>
	<nav class="navbar navbar-default">
  		<div class="container">
	    <!-- Brand and toggle get grouped for better mobile display -->
	    <div class="navbar-header">
	      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
	        <span class="sr-only">Toggle navigation</span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	      </button>
	      <a class="navbar-brand" href="/login">CoinFlip</a>
	    </div>

	    <!-- Collect the nav links, forms, and other content for toggling -->
	    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
	      <ul class="nav navbar-nav target-active">
	      	{% if session.userIsLogged == true %}
		        <li class="active"><a href="/coinflip">START GAME</a></li>
		        <li><a href="/statistics">STATISTICS</a></li>
	        {% endif %}
	      </ul>
	      <ul class="nav navbar-nav navbar-right">	
	      	{% if session.userIsLogged == true %}
                <li id="balanceAmount">BALANCE : <span id="showBalance">{{amountDollar}}</span></li> 
	      		<li id="betBtn">
		      		<label for="amountBet">BET : </label>
		      		<span>
		      			<button type="button" id="plusBtn" class="btn btn-default smallBtn">&#43</button>
		      		</span>
	      				<input type="number" name="amountBet" id="amountBet" min="0" max="{{amountDollar}}" value="{{(amountDollar / 10) | round}}">
	      			<span>
	      				<button type="button" id="minusBtn" class="btn btn-default smallBtn">&#45</button>
	      			</span>
	      		</li> 
	        	<li><a href="/logout">Logout {{session.userIsLogged}}</a></li>                    
            {% else %}
         		<li><a href="#">Buy me a Beer!</a></li>
            {% endif %}
	      	
	      </ul>
	    </div><!-- /.navbar-collapse -->
	  </div><!-- /.container-fluid -->
	</nav>

	<!-- START OF CONT-->
	<div class="container">